{
    "compilerOptions": {
      "lib": ["ES2015"],
      "module": "CommonJS",
      "outDir": "./dist",
      "rootDir": "./src"
    },
    "exclude": ["node_modules"],
    "extends": "tsconfig/base.json",
    "include": ["src"] 
}

// q: What's wrong with this tsconfig.json?
// a: It's missing the "include" property, which is required for the "extends" property to work.
